#include<stdio.h>
#include<conio.h>
void main()
{
	void swap(int,int);
	int a,b,r;
	clrscr();
	printf("Enter value for a&b: ");
	scanf("%d%d",&a,&b);
	swap(a,b);
	getch();
}
void swap(int a, int b)
{
	int temp;
	temp = a;
	a=b;
	b=temp;
	printf("After swapping the value for a & b is: %d %d",a,b);
}
